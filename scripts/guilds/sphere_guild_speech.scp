[SPEECH spk_guildspeech]
ON=I resign from my guild
IF ((<ARGN1> == <DEF.TALKMODE_SAY>) && <GUILD>)
    f_guildsys_removecharfromguild <guild.uid>,<uid>
    RETURN 6
ENDIF
RETURN 1

ON=*
IF ((<ARGN1> == <DEF.TALKMODE_GUILD>) && <DEF0.GUILDCFGSPEECH_GLOBAL_ENABLE>)
    // TODO: check if member/candidate call function to broadcast smsg globally
    RETURN 1
ENDIF

[EOF]