extraction:
  cpp:
    prepare:
      packages:
        - libmysql++-dev:i386
        - gcc-multilib
        - g++-multilib
    after_prepare:
      - sudo dpkg --add-architecture i386
      - sudo apt-get update
    index:
      build_command:
        - make NIGHTLY=1